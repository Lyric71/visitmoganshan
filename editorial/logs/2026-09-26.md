# Publication run, 26 September 2026

Scheduled publish run, started 03:30 Shanghai. Model: Opus 5.5, attempt 1 of 4
in the fallback chain. The prompt SHA-256 was checked against the manifest
(8D37B002...F72D5B) and matched.

## Recovery and scope

The runner log held only the start line and the attempt line, and the working
tree held no half moved guide file for this row, so the run started from the
beginning. One row was due: 005, Yucun, `/moganshan/villages/yucun/`, status
`image_ready`.

Other changes already in the working tree were left out of this commit because
the piece did not touch them: `editorial/scripts/run-daily.ps1` and
`run-news.ps1` (the runner's model order), `editorial/news/runs.json` and the
21 and 24 September news triage files. They belong to the runner and the news
layer.

## The quality gate

Row 005 carries `quality_passed_on` 2026-09-25, so it has been through
`/content-quality-us` and is eligible.

## The move

`output/yucun.md` lines 1 to 391 copied to
`src/content/guide/moganshan-villages-yucun.md`, the flat name from the
frontmatter `url`. The three appended HTML comment blocks (image instructions,
schema, asset brief) were stripped; everything else is verbatim. `published`
and `last_updated` were already 2026-09-26, the row's `publish_date`, so no
date was edited.

The four images, `public/images/guide/yucun.webp` and `-2`, `-3`, `-4`, exist
and are referenced: the lead from the frontmatter `image`, the other three as
captioned body figures. Each was looked at before the checks: candid, overcast
or dusk light, clutter and puddles, people only from behind or at a distance,
no legible text.

## Validation outcome

* `npm run img:audit`: passed, 2,749 images, all within caps.
* `npm run dates:check`: passed, 86 articles.
* `npm run check`: passed, 0 errors, 0 warnings, 8 hints.
* `npm run build`: passed; `dist/client/moganshan/villages/yucun/index.html`
  emitted.

Row 005 set to `published` with `published_on` 2026-09-26. Commit and push
follow; the commit hash is in the publish email.

## Open items carried into the email

* **TODO affiliate slug `stays-village-yucun`.** Not in `PARTNER_LINKS`; the
  piece links five property deep links instead.
* **`/moganshan/villages/` should link this page from its Yucun section.** Not
  edited here: a core piece has no `affects_pages` step, and editing that page
  would move its date. It also still carries US spellings and punctuation
  hyphens, flagged in the draft log for a person.
* **Brief correction recorded in the row:** the Republic library's entry terms
  are unpublished since the June 2026 change of operator.
